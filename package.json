{
  "name": "lab-report-server",
  "version": "0.1.0",
  "private": true,
  "description": "lab-report commercial server (v0.10.0 Commercial Core) — Node 24 + Fastify 5 + better-sqlite3 (WAL), per docs/COM-CONTRACT.md",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "migrate": "node scripts/migrate.js",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "fastify": "^5.12.3"
  }
}
