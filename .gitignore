# dependencies
node_modules/

# runtime data (SQLite + WAL sidecar files)
data/
*.db
*.db-wal
*.db-shm

# logs
logs/
*.log
npm-debug.log*

# env / secrets (never commit)
.env
.env.*

# 运营静态资源（收款码等个人物料，只放服务器不入 git）
static/

# os junk
.DS_Store
Thumbs.db
