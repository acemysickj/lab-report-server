# .npmrc — lab-report-server
# better-sqlite3 13.x 自带全平台 prebuild（含 win32-x64.node），无需任何原生编译；
# 但 npm ci/install 会对含 binding.gyp 的包隐式触发 `node-gyp rebuild`，本机/部分部署机
# 无 MSVC VC++ 工具链时会失败。ignore-scripts=true 跳过依赖包生命周期脚本
# （npm start/test/run-script 等显式命令不受影响）。
# 注意：未来若引入真正需要 install 脚本的依赖，需重新评估本设置。
ignore-scripts=true
